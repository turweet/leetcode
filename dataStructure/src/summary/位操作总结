
    byte (8 bit)
    short (16 bit)
    int (32 bit)
    long (64 bit)
    and even char (16 bit)

Java提供的位运算符有：左移( << )、右移( >> ) 、无符号右移( >>> ) 、
位与( & ) 、位或( | )、位非( ~ )、位异或( ^ )，
除了位非( ~ )是一元操作符外，其它的都是二元操作符。

异或操作：相同则结果为0，不同则结果为1